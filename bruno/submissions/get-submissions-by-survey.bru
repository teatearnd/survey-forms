meta {
  name: Get Submissions by Survey
  type: http
  seq: 2
}

get {
  url: {{base_url}}/survey/:surveyId/submissions
}

params {
  surveyId: {{survey_id}}
}

headers {
  Authorization: Bearer {{auth_token}}
}
